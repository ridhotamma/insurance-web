import styled from "styled-components";

const Navbar = styled.nav`
    width: 100%;
    padding: 20px;
    display: flex;
`;

const Logo = styled.div``;
const Items = styled.ul`
    display: flex;
`;
const Item = styled.li`
    font-size: 20px;
`;

export { Navbar, Items, Item, Logo };
